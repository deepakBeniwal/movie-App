import React from 'react';
import MovieSearch from './MovieSearch';
import Cards from '../components/Cards/Card';

const Home = () => {
    return (
        <div>
          <MovieSearch/>

            <Cards />
        </div>
    );
};

export default Home;
     