import React from 'react';
import playBtnIcon from '../../assets/playBtn.svg';

const PlayButton = () => {
    return (
        <div className="btnIcon">
            <img src={playBtnIcon} alt="play button" />
        </div>
    );
};

export default PlayButton;
